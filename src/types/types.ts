export type ClientDB = {
    _id: string;
    nombre: string;
    email: string;
    telefono: number;
    notas: string;
}

export type ClientForm = {
    nombre: string;
    email: string;
    telefono: number;
    notas: string;
}
